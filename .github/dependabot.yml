version: 2
updates:
  # npm version PRs are off. Expo SDK 55 pins React Native, React 19.2,
  # flash-list, screens, TypeScript 5.9; weekly bumps fail expo-doctor.
  # Dependabot security alerts still come from GitHub repo settings.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
